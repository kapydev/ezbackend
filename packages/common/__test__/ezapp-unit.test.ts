

describe("EzApp Unit Tests", () => {

  describe("When using defaultOpts", () => {
    test.todo("If default opts are defined it should return the default opts")
    test.todo("If default opts are not defined it should throw an error")
  })

  describe("Building Route Prefix", () => {

    test.todo("If there is no plugin prefix it should return the instance prefix")
    test.todo("Building Route Prefix should handle having slashes at the end of prefix and start of next item")
  })
})

export {}